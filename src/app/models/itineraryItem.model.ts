export interface ItineraryItem {
  _id: string | null,
  costEstimate: string | null,
  currency: string | null,
  description: string | null,
  endDateTimeISOString: string | null,
  itineraryId: string | null,
  notes: string | null,
  startDateTimeISOString: string | null,
  tag: string | null,
  title: string | null,
}
